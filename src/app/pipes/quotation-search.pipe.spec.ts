import { QuotationSearchPipe } from './quotation-search.pipe';

describe('QuotationSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new QuotationSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
